import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brownie.worksessiontracker',
  appName: 'WorkSessionTracker',
  webDir: 'dist'
};

export default config;
